### JWT Token 객체의 행위???
- 유효성 확인
- noSQL에 조회해서 유효성확인
- 새거 발급?? (Refresh로부터)
  - 발급할 때 UUID 발급해서 갱신
  - 갱신할때 noSQL 갱신해야함
- noSQL에서의 세션 데이터 삭제?
  - Token의 상태 변경할때 삭제되도록 하자

- Token의 status
  - Request에서 받아와서 인증하기 전 (막 생성됨)
  - 인증됨
  - 만료됨
    - noSQL에서 삭제하도록

- Access 토큰 객체
  - 그냥 인증만 할줄알면됨
  - 만료시 할거없음
- Refresh 토큰 객체
  - 받은 정보를 통해 새로운 Refresh/Access 토큰 객체 생성

- 둘 다 내부 엔티티 정보를 바꿀 일이 없어 보이는데 불변으로 합시다
  - Access의 경우 추후 Claim 변경 가능하지 않을까 싶은데 일단 나중에 생각

- Token 객체는 영속화하지 않음
  - 토큰 저장할거야???
- 단, 도메인 로직에서 noSQL 데이터의 읽고 쓰기를 할 뿐이다
  - 이러면 key-value의 도메인이 따로 필요한가?
  - Session-UUID 도메인이 존재하는 것이 맞나?
  - 도메인 로직이 있긴 함. UUID 변경, Duration 변경, 삭제
  - JWT 도메인에서 S-U에게 원하는 로직도 있긴 함
    - 존재하는지 아닌지 확인
    - 내가 가져온 UUID가 S-U가 가지고 있는 UUID와 동일한지 확인

### 액세스와 리프레시의 차이?
- 리프레시 부정사용을 알아내기 위해서는, 결국 액세스 토큰을 사용해 유효성을 검증할 때 SessionID와 UUID 둘 다 알아야하는거 아닌가?
  - 1만 알면 UUID의 변경을 알수없고
  - 2만 알면 UUID 변조를 알았을 때 해당 로그인의 세션 정보를 알수가없다!
- 그럼 그냥 유효기간만 다른 두 토큰..?
- 아마도 정보 저장만 차이로 두면 될듯? Refresh Token은 사용자 ID 정도만 들고있으면 되겠고, 자주 갱신되는 Access의 경우 좀 더 최신의 정보, 혹은 서비스에서 필요한 정보?

### Claim을 객체로서 관리하려면 ???
- 인터페이스 두고 구현한 객체 만들어서 관리할까??
  - 그러면 인터페이스의 구현체 정하는건 어디서 ?


### 동시성 충돌의 가능성
- 동시에 Refresh Token 사용하는 경우?
- 어차피 Refresh Token을 동시에 소비하는 경우가 잘못된 셈인데 필요한가싶고??
  - 구현한다면 나중에 할것 redis pub-sub lock 하면 될 것 같긴 함


### 토큰 객체의 대충 구조?
- String 토큰 값
- Claims 객체 -> 여러 객체를 상속?
- Duration


### 행위
- String으로부터 claim을 받아서 mapping
- 입력받은 시간을


### JWT facade layer에서 할것
- access 토큰 인증
- refresh token이 이미 사용되었을 때, session id를 무효화

- 생각해보니까 이 2개만 있으면되네..? 토큰 재발급은 인증 레이어가 할게 아니네...?????????
  - 그러면 토큰 생성 로직은 아예 덜어내면 되고
  - RefreshToken 계열 관리할 필요 없고

- 음 근데 기술실증용 프로젝트니까 별도의 rest controller에 만들어만 둡시다!!!!!
  - 로그인은 빼버리고 (ID 비밀번호 비교는 제하고, 신규 세션 발급만 만들어둘것)
  - 세션 시작, 로그아웃, Refresh token 사용 이 3개만 만들것
  - 별도의 패키지로 분리하여 만들것


### 로그인/로그아웃/Refresh 소비에서 해야 할 작업
- ID를 받아서 Refresh/Access token 발급
- Access Token을 받아서 세션 종료할것 (Redis에서 세션 Key pair 삭제)
- Refresh token을 사용해서 새로운 Refresh/Access Token 발급할 것
  - 여기서는 유출 여부를 체크할 필요가 없다고.. 생각한다. 왜냐하면 그 전에 Gateway를 거칠 것이기에 거기서 유출 체크와 후처리를 진행할 것이므로